- on the food search screen, when you tough the "Search the Web" toggle, it doesn't toggle.  This MAY be happening because the search entry has focus, because I can get around it by tapping the scan button and then backing out, causing the search entry to lose focus.  once it was toggled over, i couldn't toggle it back.  this issue was probably introduced when we fixed a bug wherein while toggled to "search the web", hitting the enter key would automatically end the search and set the toggle back to "my foods", so we should make sure not to reintroduce that bug.
- when searching "My Foods" the log button is redundant as we can already tap on the food list entry itself.  however, the behavior of the button is preferable.  it opens the drawer without exiting the food search view, and only returns to the log tab once the user hits save.  this is what should happen when we tap on the list entry, and we should remove the redundant button.
- on the log screen, we should remove the macros and the log time from the logged food entries.  then, we should add bars under the food that represents its macro breakdown by calorie.  so the user gets a visual representation of the breakdown of the given food.  so it should be 3 bars in series that add up to the full width of the food item.  for example, if a food had 1g carb, 1g protein, 1g fat, that's 4+4+9 calories, so 17.  so the blue (protein) portion of the bar should be 4/17 of the width of the screen, same for carbs, and the portion of the bar representing fat should be 9/17 the width of the screen.
- on the log screen, the date text in the top bar should stay centered, but the control arrows on either side of it should stay tight to it rather than spreading horizontally.
- on the log screen, we should remove the Breakfast/Lunch/Dinner/Snack badges, as they're redundant with the existing section markers.  the section markers should be made to visually pop a bit more.  we should move the calories for each food entry to the right, near to where the badges are now.
